gpg-connect-agent updatestartuptty /bye > /dev/null
set -x SSH_AUTH_SOCK (gpgconf --list-dirs agent-ssh-socket)
set -x GPG_TTY (tty)
set -x RIPGREP_CONFIG_PATH $HOME/.config/rg/config
set -x HOSTNAME (hostname)
set -x PNPM_HOME $HOME/.local/share/pnpm
set -x PATH "$PATH:$PNPM_HOME:$HOME/.local/bin:$HOME/.cargo/bin:$HOME/.luarocks/bin"
set -x BUN_INSTALL "$HOME/.bun"
set -x PATH $BUN_INSTALL/bin $PATH

{{ if ne .chezmoi.os "darwin" -}}
set -x GRIM_DEFAULT_DIR $HOME/Media/Pictures/Captures
set -x PERSONAL $HOME/Personal
set -x SUPERMEMORY_API {{ output "show" "supermemory.ai/prncss-xyz" | trim }}
set -x MORPH_API_KEY {{ output "pass" "show" "morphllm.com/quarknoir@gmail.com" | trim }}
set -x EXA_API_KEY {{ output "pass" "show" "exa.ai/juliette.lamarche.xyz@gmail.com" | trim }}
set -x TAVILY_API_KEY {{ output "pass" "show" "talivy/prncss-xyz" | trim }}
{{ end -}}
