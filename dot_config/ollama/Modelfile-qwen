FROM qwen2.5-coder:1.5b

# GPU layers to offload
PARAMETER num_gpu 99

# Batch sizes
PARAMETER num_batch 1024

# Context size (llama-server 0 usually means model default, 32768 for Qwen2.5)
PARAMETER num_ctx 32768

# Flash Attention is typically a server-side flag (OLLAMA_FLASH_ATTENTION=1)
# but we can ensure the template or other parameters match if needed.
