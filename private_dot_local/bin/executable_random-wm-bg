#!/bin/sh

wallpaper_dir="$HOME/Personal/wallpapers"

while true; do
	img=$(fd -t f . "$wallpaper_dir" | shuf -n 1)
	if [ -f "$img" ]; then
		# swaymsg output "*" bg "$img" fill
		swaybg -i "$img" -m fill
	fi
	sleep 1200
done
