#!/usr/bin/env bash

REPOS=(
  "$HOME/projects/notes/main/"
  "$HOME/projects/nvim/main/"
  "$HOME/projects/dotfiles/"
  "$HOME/Personal/pass"
)

for repo in "${REPOS[@]}"; do
  cd "$repo" || continue
  git-sync
done

chezmoi apply
