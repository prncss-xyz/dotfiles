#!/usr/bin/env bash

REPOS=(
  "$HOME/projects/notes/"
  "$HOME/projects/nvim/"
  "$HOME/projects/dotfiles/main"
)

for repo in "${REPOS[@]}"; do
  cd "$repo" || continue
  git-sync
done

chezmoi apply
